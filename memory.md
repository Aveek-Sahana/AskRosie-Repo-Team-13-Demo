# Crystal Bridges candidate artwork catalog

The exact 100-work candidate list supplied for AskRosie image classification is stored in `src/artworkCatalog.json`. The classifier uses that catalog as a closed candidate set; it should not claim a match outside it.
